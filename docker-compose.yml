version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - '8001:8001'
    env_file:
      - ./backend/.env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    command: redis-server --appendonly yes